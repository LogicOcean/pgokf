# 0.3.0 release fix-up validation — 2026-09-22

Validated source commit: `5aa69eec3e28007530e7529c28d3fb290c5ea8e7`.
The commit adding this report changes documentation only. It does not change
any tested source, dependency, SQL, workflow, formula, or Docker build input
(`docs/` is excluded from the Docker context). Independent review must examine
the final branch tip; this report is not a merge or publication approval.

Fix-up commits:

- `c9c64008af604141a6556e9600d42263989a9d01`: preserve the inherited work
  before expensive gates; explicitly recorded incomplete validation.
- `5d2985973c89b636c08c9270138471599ab093c8`: publication identity, SQL
  inventory completeness, standalone cleanup, Homebrew sequence, and regressions.
- `5aa69eec3e28007530e7529c28d3fb290c5ea8e7`: five upstream doc-comment
  whitespace corrections and explicit dispatch input type.

## Review findings

| Finding | Disposition | Evidence |
| --- | --- | --- |
| F1 Homebrew | FIXED | Pre-tag formula retains the published 0.2.0 URL, verified archive digest, and both 0.2.0 assertions. Unknown/current placeholder digests and mixed tuples fail. Post-tag tuple and complete rollback are tested. Fixed a separately reproduced shell-quoting defect in the formula's SQL test. |
| F2 publication | FIXED | Prep proves tag/HEAD/control/workspace/first-party lock/pin/PGXN identity. All source builds use the resolved commit SHA. Current resolver/validation tooling is separate from historical source. Branch dispatch with the old publication boolean produces `publish=false`; explicit tag catch-up works. Manifests require both architectures and successful build matrices. |
| F3 audit | FIXED | Root resolved graph contains no serde_cbor. No advisory ignore remains. Both strict audit executions report zero advisories/warnings. A project-scoped pgrx 0.19.2 patch removes unused CBOR custom-type helpers; source archive, upstream revision, MIT license, exact delta, compatibility boundary, and update policy are documented in `vendor/pgrx/VENDORING.md`. |
| F4 documentation | FIXED | Required PG15–19 matrices are distinguished from previously published 0.2.0 artifacts. Current Docker examples use 0.3.0. Historical upgrade references remain. |
| F5 verification | FIXED | All 16 requested local gate groups completed below; no failed gate is waived. |
| F6 guards/parity | FIXED | Release identity, SQL graph/receipt, workflow, Homebrew, dependency and cleanup tests run in CI. All 13 exact file mutations fail. Live wrong-version and missing-role-bootstrap mutations fail and restore the generated install file. |

## Completed local gates

Rust 1.96; native macOS and isolated Linux containers on arm64; live PostgreSQL
18. These results do not claim live PG15–17/19, x86_64, Homebrew installation,
RPM installation, remote CI, or publication execution. Required matrix and
provisioning failure paths are tested statically and executably by the CI
coverage guards. The post-tag Homebrew install/test remains a publication step.

| # | Gate | Result |
| --- | --- | --- |
| 1 | `cargo fmt --all -- --check` | PASS |
| 2 | PG18 extension Clippy, all targets, `--locked`, `-D warnings` | PASS |
| 3 | Nonextension workspace Clippy, all targets, `--locked`, `-D warnings` | PASS |
| 4 | `scripts/test-workspace.sh --quiet` | 822 passed, 0 failed, 0 ignored |
| 5 | Linux extension unit/API tests | 237 + 18 = 255 passed, 0 failed, 0 ignored |
| 6 | PG18 release pgrx suite | 399 + 18 = 417 passed, 0 failed, 0 ignored |
| 7 | Same release suite with `PGOKF_TEST_PRELOAD=pg_textsearch` | 399 + 18 = 417 passed, 0 failed, 0 ignored |
| 8 | Two independent schema generations; generated/generated and generated/committed comparisons; install | Both comparisons equal: 154 SQL entities, 902 top-level statements; installation passed |
| 9 | `scripts/upgrade-parity.py` | 1,944 catalog rows; standalone committed bootstrap; 10 populated upgrade routes; 49 atomic hostile refusals; 28 rejected catalog mutations |
| 10 | `cargo deny check --hide-inclusion-graph` | PASS: advisories, bans, licenses, sources; existing duplicate-version notices remain under the configured bans policy |
| 11 | Configured and independent-directory `cargo audit --deny warnings` | Both PASS; 474 resolved packages, 0 advisories, 0 warnings, empty ignore list |
| 12 | `cargo about generate --workspace --all-features` and byte comparison | PASS, byte-identical notices |
| 13 | CI coverage + release integrity + SQL/cleanup tests | 3 + 18 + 5 = 26 passed; additionally 13 exact file mutations rejected |
| 14 | Fresh `docker build --no-cache`, PG18, `PGOKF_VERSION=0.3.0`; smoke/backup/restore | PASS; restored 1 bundle and 4 concepts, policy preserved, health true |
| 15 | Matching/mismatched tag, branch dispatch, explicit historical catch-up | PASS; real `v0.2.0` resolves to `59d29d3dc4b78cd5c5000bf473b0f097c9fb2ea4`; annotated-tag and package-drift fixtures also pass |
| 16 | Whole-branch diff/public-hygiene scans and resource cleanup | PASS; no tracked caches, personal paths, credentials, private keys, or conflict markers detected; private-range hits were inspected public examples/network fixtures |

Counts overlap between suites and must not be summed as unique tests.
The 399-test release suite includes the 237 host unit tests and 162 database
wrappers. The plain run permits optional-provider absence; the preload run
requires pg_textsearch and exercises its provider behavior.

The parity inventory covers function bodies, role attributes and memberships,
owners, ACLs, extension membership, relation/column/type definitions, constraint
and trigger enforcement, policies, rules, default privileges, comments, initial
privileges, and dump registration. Data fingerprints and freshness checks cover
the populated routes. Failed adoption compares pre/post catalogs and versions
for transaction rollback. Dropping the generated database and its cluster roles
before committed bootstrap prevents borrowed-role false positives.

SQL comparison ignores entity ordering and source comments, preserves quoted
contents, and includes executable SQL outside generated markers. It supplements
rather than replaces live semantics. Additional live mutations appended the
reviewer's wrong version function and removed the role-creation block; both
were refused, and the installed generated SQL was restored after each failure.

The pgrx patch is verified against the published crate archive SHA256
`8d7e6f85d841cd9c01092ad3b9d60fc778bf54dc6174bbd24126250659af7d91`.
Only seven upstream files differ: three documented dependency/helper/comment
edits and four files with five trailing-space doc-comment corrections. The
upstream license is retained byte-for-byte. Metadata proves the workspace uses
the local patch; Docker copies it; deb/RPM/Homebrew builds use the workspace
root patch. No workspace Rust source uses `PostgresType`; adding it requires
revisiting this explicitly limited patch. The current upstream develop manifest
still declared serde_cbor when checked, so changing to it would not close the
advisory graph.

Reproductions and complete stdout/stderr logs were retained outside the public
source tree, including RED tests for unknown Homebrew digests, tag-name output,
SQL text omitted by the old comparator, literal whitespace, malformed blocks,
cleanup branches, and Homebrew shell quoting. Final passing logs are separate
from those intentional failures. Scratch clusters stopped; this run's container,
three local image tags, and identified anonymous volumes were removed. No
unrelated containers, volumes, images, or shared build caches were pruned.

## Publication handoff

Follow the exact commands and rollback in
[Homebrew formula and tap](../release-checklist.md#homebrew-formula-and-tap):

1. After independent review, create the immutable `v0.3.0` source tag. Its
   formula remains a runnable, authenticated 0.2.0 consumer.
2. Fetch the tag archive with HTTP failure checking; inspect its source identity
   and record the actual archive SHA256. Never hash a local repack or move the tag.
3. In a separate post-tag main commit, advance formula URL, digest, both version
   assertions, release-state comment, and the authenticated digest record in
   `tests/test_release_integrity.py` together. Run the release guards.
4. Copy the identical formula to `LogicOcean/homebrew-pgokf/Formula/pgokf.rb`;
   run audit, source install, and `brew test` against the local tap. Publish main,
   then the tested tap update, and record both commits.
5. On failure, retain or revert the entire last verified URL/digest/assertion
   tuple in main and the tap. Do not retag or publish a mixed tuple.

The documented GitHub release body extraction was executed locally and produced
247 lines, excluding the release heading, Unreleased, and subsequent release
sections. The publication command itself was not run:

```bash
awk '/^## \[0\.3\.0\]/{emit=1; next} emit && /^## \[/{exit} emit {print}' \
  CHANGELOG.md > /tmp/pgokf-0.3.0-release.md
gh release create v0.3.0 --verify-tag --title 'pgokf 0.3.0' \
  --notes-file /tmp/pgokf-0.3.0-release.md
```

No push, release tag, GitHub release, publication, deployment, or production
modification was performed. Temporary Git tags existed only in disposable test
fixtures. A fresh independent review of the final exact tip remains required.
